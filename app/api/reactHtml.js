import React, { PropTypes } from 'react';

const ReactHtml = (props) => {
	return (
		<div>
			<p>YUSUF</p>
		</div>
	);
};

ReactHtml.propTypes = {};

ReactHtml.defaultProps = {};

export default ReactHtml;
